function insert(num)
{
  //document.getElementByClassName('result').innerHTML = num;
  function insert(num) {
    const resultElement = document.getElementsByClassName("result")[0];
    resultElement.innerHTML = num;
  }
  
}